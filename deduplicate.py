with open("crates/vac_cli/src/tui/controller.rs", "r") as f:
    content = f.read()

# I'll just use a python script to refactor this.
# But it's easier to manually replace the blocks with a function call.
