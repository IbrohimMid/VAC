# vac_core fuzz targets

Targets:

- `bundle_import`: exercises bundle JSON parsing and validation.
- `policy_gate`: exercises shell command classification.

Run from `crates/vac_core/fuzz/` with `cargo fuzz run <target>`.
