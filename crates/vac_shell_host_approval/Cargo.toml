[package]
name = "vac_shell_host_approval"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Step 2 slice 4 — VAC-owned approval queue + decision state. Implements vac_shell_bridge::ApprovalController. Independent of vac_tui_runtime."

[dependencies]
vac_shell_bridge = { path = "../vac_shell_bridge" }
vac_shell_approval_bar = { path = "../vac_shell_approval_bar" }

[dev-dependencies]
