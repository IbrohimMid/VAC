//! Submit orchestration — placeholder for F2.4.
//!
//! This module will host `submit_one`, the function that binds
//! [`crate::transcript::TranscriptWriter`], [`crate::slash::SlashProcessor`],
//! [`crate::compact::CompactBoundary`], and [`crate::usage::UsageTracker`]
//! into a single durable submit lifecycle.
//!
//! Kept as a stub for F2.0 so downstream crates can depend on the
//! crate now and consume submit in a follow-up commit.

// Placeholder — real implementation lands in F2.4 once the individual
// primitives are proven stable in isolation.
