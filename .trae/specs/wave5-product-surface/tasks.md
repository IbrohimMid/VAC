# Tasks

- [x] TUI: Implementasi fungsi revert file dan bulk revert pada panel review (saat ini hanya berupa stub yang mencetak pesan).
- [x] CLI: Implementasi integrasi persetujuan (approval) secara interaktif untuk `vac run` (saat ini menggunakan dummy channel) dan terapkan permission mode yang konsisten di semua entrypoint (termasuk ACP).
- [x] CLI: Selesaikan implementasi placeholder "future" pada command `vac export` (CBOR dan Claude JSONL).
- [x] Task 5: Fix failing checkpoints
  - [x] SubTask 5.1: Wire up TUI review panel with bulk action and restore per-file properly.
  - [x] SubTask 5.2: Make Permission mode visible and uniform across all entrypoints (vac run, vac interactive, etc.).
