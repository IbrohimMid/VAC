- [x] **H1.1 Exit**: CI job `mutation-secret-detector` green; report committed at `docs/audits/mutants-secret-detector.md`.
- [x] **H1.2 Exit**: workflow `.github/workflows/fuzz-weekly.yml` exists; seed corpus committed at `crates/vac_core/fuzz/corpus/bundle_import/`.
- [x] **H1.3 Exit**: `crates/vac_core/tests/policy_gate.rs` has >= 100 corpus assertions; mutation report committed.
- [x] **H1.4 Exit**: `docs/THREAT_MODEL.md` expanded and `docs/SECURITY.md` added. Both files linked from README.
- [x] **H2.1 Exit**: `crates/vil_swarm/tests/fsm_proptest.rs` exists; `cargo llvm-cov -p vil_swarm` report shows >= 90% coverage for FSM.
- [x] **H2.2 Exit**: `docs/RUNTIME_QUEUE_BOUNDARY.md` decision logged; migration runner and back-compat test added if merged.
- [x] **H2.3 Exit**: `cargo check --workspace` is green with no duplicate `LlmConfig` definitions; trybuild test added.
- [x] **H3.1 Exit**: Grep guard test exists in `crates/vac_core/tests/engine_provider_isolation.rs`.
- [x] **H3.2 Exit**: StreamParityContract harness + table exists; CI smoke green for >= 3 providers.
- [x] **H3.3 Exit**: Config swap integration test accurately asserts dynamic reload or documented engine restart requirement.
- [x] **Phase 4 Exit**: 7 consecutive days of green required jobs on `main`; coverage report is public.
- [x] **Phase 5 Exit**: >= 4 signed channels live; per-channel smoke matrix is green; schema migration tested on 2 legacy versions.
- [x] **Phase 6A Exit**: Observability stack live in self-hosted demo (`infra/observability/`); crash hook tested; trace redaction corpus passes.
- [x] **Phase 6B Exit**: 4-week stability streak achieved; >= 3 case studies completed; audit delta is positive.
- [x] **Global Exit**: All PRs strictly target `main`, follow the single logical change per commit rule, and pass `cargo clippy --workspace --all-targets -- -D warnings`.